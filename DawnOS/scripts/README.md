# Scripts Directory

Store reusable helper scripts here.

Examples:

- `scripts/setup-fish.sh`
- `scripts/sync-theme.sh`
- `scripts/update-assets.sh`

These scripts are not automatically linked by `install.sh` but can be used to extend your dotfiles setup.
